export const loginModalOpen=() => {
    return(
    {
        type:"LOGIN_MODAL_OPEN"
    }
    );
}

export const loginModalDone = () => {
    return(
    {
        type:"LOGIN_MODAL_DONE",
    }
    );
}

